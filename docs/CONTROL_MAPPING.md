# Control Mapping

This repo keeps control mapping generic and catalog-agnostic while remaining compatible
with common families (e.g., configuration management, system integrity, supply-chain risk).

Artifacts:
- artifacts/templates/control_mapping.yaml
- artifacts/examples/control_mapping.example.yaml
