# Threat Model (repository view)

Threat statements are represented as evidence-bound tuples:

T = (Target/AO, Preconditions, Evidence Types, Mitigating Controls)

The canonical toy set is in:
- graphs/toy/toy_graph.json
